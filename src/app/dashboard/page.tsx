
export default function DashboardGeneratorPage() {
  // The main post generation view will now be rendered by the layout's DashboardClient.
  // This page itself can be minimal as the logic is centralized.
  return null;
}
